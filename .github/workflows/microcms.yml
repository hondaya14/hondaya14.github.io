on:
  repository_dispatch: # microCMS webhook
    types:
      - microcms

jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    
    steps:
      - name: test
        run: |
          echo "test"
